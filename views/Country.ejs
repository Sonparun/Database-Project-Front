<!DOCTYPE html>
<html lang="en">
<head>
    <title>Country</title>
    <style>
        body {
            font-family: 'Agency FB', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #000;
            margin: 0;
            padding: 0;
            color: #ffffff;
            background-size: cover;
        }

        h1 {
            text-align: center;
            color: #ffffff;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #333;
            padding: 12px;
            text-align: left;
            white-space: pre-wrap;
            font-size: 22px;
        }

        th {
            background-color: #444;
        }

        img {
            max-width: 700px;
            max-height: auto;
            height: auto;
            display: block;
            margin: 0 auto;
            border-radius: 5px;
            cursor: pointer; 
        }

        .actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin-top: 20px;
        }

        .link-center {
            text-align: center;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
            background-color: rgba(255, 255, 255, 0.3);
        }
        .link-center a {
            display: inline-block;
            margin: 0 10px;
            padding: 10px 20px;
            text-decoration: none;
            color: #fff;
            background-color: #ff0000;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .link-center a:hover {
            background-color: #3b0000;
        }

        /* Lightbox styling */
        .lightbox {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            justify-content: center;
            align-items: center;
        }

        .lightbox img {
            max-width: 80%;
            max-height: 80%;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <h1>View Country</h1>
    <table class="Country-table">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Data</th>
            <th>Pic</th>
        </tr>
        <tr>
            <td><%= Country.ID %></td>
            <td><%= Country.Name %></td>
            <td><%= Country.Data %></td>
            <td>
                <img onclick="openLightbox(this)" src="<%= Country.Pic %>" alt="Country Pic">
                <!-- Lightbox -->
                <div id="lightbox" class="lightbox" onclick="closeLightbox()">
                    <img id="lightbox-img" src="<%= Country.Pic %>" alt="Country Pic">
                </div>
            </td>
        </tr>
    </table>
    <div class="link-center">
        <a href="/updateCountry/<%= Country.ID %>">Edit</a>
        <a href="/updateCountry/<%= Country.ID %>">Delete</a>
        <a href="/Countrys">Back to List</a>
    </div>

    <script>
        function openLightbox(element) {
            var lightbox = document.getElementById('lightbox');
            var lightboxImg = document.getElementById('lightbox-img');

            lightbox.style.display = 'flex';
            lightboxImg.src = element.src;

            window.addEventListener('wheel', zoomImage);
        }

        function closeLightbox() {
            document.getElementById('lightbox').style.display = 'none';
            window.removeEventListener('wheel', zoomImage);
        }

        function zoomImage(event) {
            var lightboxImg = document.getElementById('lightbox-img');
            var delta = event.deltaY || -event.wheelDelta || event.detail;

            var scale = delta > 0 ? 1.1 : 0.9;
            lightboxImg.style.width = lightboxImg.clientWidth * scale + 'px';
            lightboxImg.style.height = lightboxImg.clientHeight * scale + 'px';

            event.preventDefault();
        }
    </script>
</body>
</html>
