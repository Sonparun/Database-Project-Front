<!DOCTYPE html>
<html lang="en">
<head>
    <title>Countrys</title>
    <style>
        body {
            font-family: 'Agency FB', sans-serif;
            background-color: #000000;
            color: #fff;
            margin: 0;
            padding: 0;
            flex-direction: column;
            min-height: 100vh;
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }

        h1 {
            text-align: center;
            color: #ffffff;
            -webkit-text-stroke: 2px black;
            font-family: 'Agency FB', sans-serif;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #333;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        th, td {
            border: 1px solid #444;
            padding: 12px;
            text-align: left;
            white-space: pre-wrap;
        }

        th {
            background-color: #222;
        }

        img {
            width: 300px;
            height: 180px;
            object-fit: cover;
            border-radius: 5px;
        }

        .actions {
            display: flex;
            gap: 5px;
        }

        .actions a {
            text-decoration: none;
            padding: 8px 12px;
            background-color: #ff0000;
            color: #fff;
            border-radius: 3px;
            transition: background-color 0.3s ease;
        }

        .actions a:hover {
            background-color: #3b0000;
        }

        .create-link {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 15px;
            text-decoration: none;
            background-color: #ffa600;
            color: #fff;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            font-family: 'Agency FB', sans-serif;
        }

        .create-link:hover {
            background-color: #ab6f00;
        }
    </style>
</head>
<body>
    <h1>Countrys</h1>
    <table>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Data</th>
            <th>Pic</th>
            <th>Actions</th>
        </tr>
        <% for (var i = 0; i < Countrys.length; i++) { %>
            <tr>
                <td><%= Countrys[i].ID %></td>
                <td><%= Countrys[i].Name %></td>
                <td><%= Countrys[i].Data %></td>
                <td><img src="<%= Countrys[i].Pic %>" alt="Country Pic"></td>
                <td class="actions">
                    <a href="/Country/<%= Countrys[i].ID %>">View</a>
                    <a href="/updateCountry/<%= Countrys[i].ID %>">Update</a>
                    <a href="/deleteCountry/<%= Countrys[i].ID %>">Delete</a>
                </td>
            </tr>
        <% } %>
    </table>
    <a href="/createCountry" class="create-link">Create New Country</a>
    <a href="/" class="create-link">Add Country Of Movie</a>
    <a href="/Movies" class="create-link">Add Movie</a>
</body>
</html>
