<!DOCTYPE html>
<html lang="en">
<head>
    <title>Update Movie And Country</title>
    <style>
        body {
            font-family: 'Agency FB', sans-serif;
            background: url('https://www.100hdwallpapers.com/wallpapers/2880x1800/penguins_of_madagascar_movie-widescreen_wallpapers.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        h1 {
            color: #f0f2f3;
        }

        form {
            width: 50%;
            margin: 20px;
            padding: 20px;
            background: rgba(40, 40, 40, 0.9);
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
        }

        label {
            display: block;
            margin: 10px 0;
            color: #eeeeee;
        }

        input {
            width: 100%;
            padding: 15px;
            margin: 5px 0;
            box-sizing: border-box;
            border: 2px solid #2196f3;
            border-radius: 10px;
            background-color: #292929;
            color: #fff;
            font-size: 16px;
            font-family: 'Agency FB', sans-serif;
        }

        input[type="submit"] {
            background-color: #2196f3;
            color: white;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #1769aa;
        }

        a {
            display: block;
            margin-top: 20px;
            text-decoration: none;
            color: #ffffff;
            font-size: 24px;
        }
    </style>
</head>
<body>
    <h1>Update Movie And Country</h1>
    <form method="post" action="/updateMovieAndCountry/<%= MovieAndCountry.MovieID %>">
        <label for="MovieID">MovieID</label>
        <input type="text" id="MovieID" name="MovieID" value="<%= MovieAndCountry.MovieID %>">
        <br>
        <label for="CountryID">CountryID</label>
        <input type="text" id="CountryID" name="CountryID" value="<%= MovieAndCountry.CountryID %>">
        <br>
        <input type="submit" value="Update">
    </form>
    <a href="/MovieAndCountry/<%= MovieAndCountry.MovieID %>">View</a>
    <a href="/DeleteMovieAndCountry/<%= MovieAndCountry.MovieID %>">Delete</a>
    <a href="/">Back to List</a>
</body>
</html>
