<!DOCTYPE html>
<html lang="en">
<head>
    <title>Movie And Country</title>
    <style>
        body {
            font-family: 'Agency FB', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #000;
            margin: 0;
            padding: 0;
            color: #ffffff;
            background-size: cover;
            flex-direction: column;
            min-height: 100vh;
            background-size: cover; /* ให้รูปครอบคลุมพื้นที่ */
            background-position: center; /* จัดตำแหน่งรูปตรงกลาง */
            background-repeat: no-repeat; /* ไม่ให้รูปซ้ำ */
        }

        h1 {
            text-align: center;
            color: #ffffff;
            font-size: 36px;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #333;
            padding: 12px;
            text-align: left;
            white-space: pre-wrap;
            font-size: 22px;
        }

        th {
            background-color: #444;
        }

        img {
            max-width: 700px;
            max-height: auto;
            height: auto;
            display: block;
            margin: 0 auto;
            border-radius: 5px;
            cursor: pointer;
        }

        .actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin-top: 20px;
        }

        .link-center {
            text-align: center;
            margin-top: 20px;
        }

        .link-center a {
            display: inline-block;
            margin: 0 10px;
            padding: 10px 20px;
            text-decoration: none;
            color: #fff;
            background-color: #ff0000;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .link-center a:hover {
            background-color: #f90000;
        }

        /* Lightbox styling */
        .lightbox {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            justify-content: center;
            align-items: center;
        }

        .lightbox img {
            max-width: 80%;
            max-height: 80%;
            border-radius: 5px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
            background-color: rgba(255, 255, 255, 0.3);
        }
    </style>
</head>
<body>
    <h1>View MovieAndCountry</h1>
    <table class="MovieAndCountry-table">
        <tr>
            <th>MovieID</th>
            <th>CountryID</th>
        </tr>
        <tr>
            <td><%= MovieAndCountry.MovieID %></td>
            <td><%= MovieAndCountry.CountryID %></td>
        </tr>
    </table>
    <div class="link-center">
        <a href="/updateMovieAndCountry/<%= MovieAndCountry.ID %>">Edit</a>
        <a href="/updateMovieAndCountry/<%= MovieAndCountry.ID %>">Delete</a>
        <a href="/">Back to List</a>
    </div>
    <!-- Lightbox for displaying larger image -->
    <div class="lightbox" id="lightbox">
        <img src="" alt="Large Image" id="lightbox-image">
    </div>
</body>
</html>
